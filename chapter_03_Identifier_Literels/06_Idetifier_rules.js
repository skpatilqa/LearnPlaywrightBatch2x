/*-Ientifiers can contain letters (a–z, A–Z), digits (0–9), underscores (_), and dollar signs ($).
- Identifiers must not start with a digit.
- Identifiers are case-sensitive.
- Identifiers cannot be reserved keywords (e.g., let, class, return).
- No spaces or special characters (like @, #, %, etc.) are allowed.
- Unicode letters and escape sequences are allowed, though ASCII is standard practice. */